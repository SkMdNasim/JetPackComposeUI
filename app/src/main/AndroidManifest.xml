<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetPackComposeUI"
        tools:targetApi="31">
        <activity
            android:name=".StateWithTextFuildActivity"
            android:exported="false"
            android:label="@string/title_activity_state_with_text_fuild"
            android:theme="@style/Theme.JetPackComposeUI" />
        <activity
            android:name=".StateManagemantActivity"
            android:exported="false"
            android:label="@string/title_activity_state_managemant"
            android:theme="@style/Theme.JetPackComposeUI" />
        <activity
            android:name=".LazyColoumActivity"
            android:exported="false"
            android:label="@string/title_activity_lazy_coloum"
            android:theme="@style/Theme.JetPackComposeUI" />
        <activity
            android:name=".ButtonActivity"
            android:exported="false"
            android:label="@string/title_activity_button"
            android:theme="@style/Theme.JetPackComposeUI" />
        <activity
            android:name=".FirstActivity"
            android:exported="false"
            android:label="@string/title_activity_first"
            android:theme="@style/Theme.JetPackComposeUI" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JetPackComposeUI">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
